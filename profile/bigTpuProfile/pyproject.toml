[build-system]
requires = ["setuptools>=61.0.0", "wheel", "setuptools_scm>=8.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "bigTpuProfile/_version.py"
fallback_version = "0.1.0"