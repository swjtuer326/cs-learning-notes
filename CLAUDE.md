# CLAUDE.md — 学习笔记写作规范

指导 AI 在本仓库中编写、修改、审核学习笔记。目标：内容连贯、图表规范、格式统一。

***

## 0. 术语与缩写

**核心规则：任何非通识性英文缩写，首次出现时必须给出全称和中文释义。**

- 通识性缩写（可不定义）：CPU, GPU, OS, API, RAM, USB, HTTP, HTML, CSS, SQL
- 其余所有缩写**必须定义**，格式：`DDR (Double Data Rate, 双倍数据速率)`
- **每篇文档**在标题下方、正文之前设置 `### 关键术语` 表格，列出本文涉及的核心缩写

***

## 1. 写作风格

- **语言**：中文为主，英文术语首次出现带括号原文。语调：工程师对工程师，直接专业。
- **禁止**：过多废话（"在当今..."、"让我们深入..."）、空洞总结（"综上所述..."）、无信息过渡句、表情符号、过度谦虚（"笔者水平有限"）。
- **鼓励**：具体数据替代抽象描述、对比帮助理解、类比降低门槛（须说明局限）、引用规范注明章节号。

### 1.1 本质先行

**每个重要概念必须先理解本质，再引入形式化描述。**

- 先用具体场景解释"这件事在做什么"，再用数学/代码/规范精确描述"怎么做"
- 场景说明须指出适用范围——读者需要知道该描述在何处失效
- 正例：先说"扩散模型的核心操作：把部分 token 替换为掩码，再从掩码恢复原始内容"，再定义前向/反向过程的数学
- 正例：先说"Cache 把常用数据放在离 CPU 更近的小容量存储中，减少访问主存的次数"，再讲 Cache 层级和命中率的数学定义
- 反例：上来就写 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon$，读者不知道这个公式在干什么

### 1.2 "为什么"解释

**关键设计选择必须解释"为什么"，不能只说"是什么"。**

- 引入新概念/新机制/新公式时，回答：为什么需要它？它解决了什么问题？不用它会怎样？
- 正例："为什么掩码比例要随机？因为推理时模型可能面对任何掩码比例——从全掩码到几乎无掩码。训练时让模型见过各种比例，它才能在多步去噪的每一步都表现良好。"
- 正例："为什么 DDR 需要预取（Prefetch）？因为 DDR 的行激活后数据只在行缓冲区中，如果不预取，每次列访问都要等 tCL 延迟——预取把连续 8 个列数据一次性取出，摊薄了延迟开销。"
- 反例："掩码比例从 $U[0,1]$ 采样。"（没有解释为什么）

### 1.3 具体数值演算

**当文档包含数学公式时，公式不应仅列出形式，应用真实数字逐步计算至少一个例子。**

- 适用场景：文档中有可计算的数学公式（如扩散过程、时序分析、功耗计算）
- 不适用场景：纯概念描述、寄存器字段定义、协议状态机等无计算公式的章节
- 给出公式后，用小规模数据走一遍完整计算过程，让读者能手动验证
- 数值例子放在公式之后、一般性讨论之前
- 正例：给出 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon$ 后，用 2×2 灰度图、具体 $\beta_1=0.0001$ 逐步算出 $x_1$ 的每个元素
- 正例：给出 DDR 行激活功耗公式后，代入具体频率和电压算出一个典型值
- 反例：只写公式，没有数值验证

### 1.4 具体小例子先行

**在解释一般性概念之前，先给出小规模、可手动验证的具体例子。**

- 例子规模要小到读者能在脑中或纸上验证（如 3-5 个 token、2×2 像素、4 个寄存器字段、3 步握手流程）
- 例子必须完整——输入、过程、输出都给出，不留悬念
- 之后再推广到一般情况

### 1.5 对比驱动理解

**当引入新概念时，优先通过与读者已知概念的对比来建立理解。**

- 对比表格第一列为"对比维度"或"维度"
- 对比维度应覆盖关键差异（如：输入/输出、训练目标/功能、推理方式/工作模式、优劣势）
- 正例：引入离散扩散时，与 BERT 的 MLM 对比（掩码比例、注意力、预测目标、用途）
- 正例：引入 DDR5 时，与 DDR4 对比（Bank 数、突发长度、供电电压、速率范围）

***

## 2. 文档结构

### 2.1 章节文档骨架

```markdown
# 标题

> 一句话概括本文内容。
> **工程师视角**：（可选）实践角度补充。

### 关键术语
| 缩写 | 全称 | 含义 |
|------|------|------|
| XX | Xxxxx Xxxxx | ... |

---

## 1. 概述

### 1.1 前置知识
| 需要了解 | 参考文档 |
|----------|----------|
| XX | [链接](./xx.md) |

（正文，每 300-500 字插入视觉元素：图/表/代码）

---

## 参考资料
- [名称](URL) — 参考了什么
```

### 2.2 章节桥接段

**每个主要章节（H2）开头应有一段桥接引用块**，完成三个任务：

1. **承接上文**：一句话概括上一章的核心结论
2. **说明动机**：为什么需要这一章？上一章留下了什么问题？
3. **预告内容**：本章将讲什么

格式：

```markdown
> 上一章建立了……的核心框架。一个自然的问题是：……？本章用……来回答这个问题——先讲……，再讲……。
```

正例：

```markdown
> 第二章讲了图像扩散：像素是连续实数，可以直接加高斯噪声。但文本是离散的——"猫"和"狗"之间没有中间状态。研究人员的第一反应是：能不能把文本变成"连续"的？这就是连续扩散语言模型的思路。
```

正例：

```markdown
> 上一章介绍了 DDR 的基本读写操作。但实际系统中，CPU 不会每次只访问一个 cache line——连续访问、页面命中/缺失、Bank 交错等因素共同决定了真实带宽。本章从时序图出发，分析 DDR 的带宽利用率。
```

反例：章节开头直接进入正文，读者不知道为什么突然切换话题。

### 2.3 渐进复杂度

**文档整体结构应从简单到复杂逐层递进。**

- 先理解本质（具体场景、小例子），再引入形式化描述（数学/规范/代码），再讨论工程实现
- 先讲最简单/最基础的方案，再讲优化和变体
- 每一层只在前一层完全建立后才引入新复杂度
- 正例：本质 → 图像扩散（连续，数学最简） → 连续文本扩散（引入 rounding 问题） → 离散文本扩散（主流方案） → 大规模模型
- 正例：SRAM 基本单元（6T 结构） → 读写操作 → 位线预充与灵敏放大 → 多端口 SRAM → Cache 组织方式

### 2.4 导航文档（README.md）

包含：一句话定位 + Mermaid 学习路线图 + 文档索引表（序号/文档/概要/建议学时）。

### 2.5 导航链

每篇文档开头有"前置阅读"，结尾有"下一篇"链接，保持双向可达。

***

## 3. 标题层级

```
# 文档标题          ← H1，每文件仅一个
## 1. 大节标题      ← H2，每篇 5-8 个
### 1.1 小节标题     ← H3，每个 H2 下 2-4 个
#### 深入标题        ← H4，谨慎使用
```

- 禁止 H5+——拆为新 H3 或降为加粗文本
- 同一专题内编号风格统一（阿拉伯数字 `## 1.` **或** 中文序数 `## 一、`，不可混用）
- 标题末尾不加标点，不使用反引号

***

## 4. 表格

- 表头**加粗**，内容关键词加粗
- 表格前后各空一行
- 对比类表格第一列为"对比维度"
- 超 8 行考虑拆分

### 4.1 "如何读这张表"引导

**当表格包含多列数值或需要特定解读方式时，必须在表格后附一段阅读指引。**

- 用引用块格式：`> **如何读这张表**：……`
- 指出关键列的含义、数值的物理意义、读者应关注的趋势或对比
- 正例：`> **如何读这张表**：$\sqrt{\bar{\alpha}_t}$ 是原图信号在 $x_t$ 中的权重，$\sqrt{1-\bar{\alpha}_t}$ 是噪声的权重。$t=100$ 时信号占 $88\%$……`
- 反例：复杂表格后没有任何解释，读者自行摸索

***

## 5. 代码块

- **必须**标注语言：`c`, `asm`, `bash`, `python`, `ld`, `yaml`, `json`, `dts`, `kconfig`
- 前后各空一行，超 30 行应拆分或标注省略
- 关键变量/函数在正文中有文字解释
- **自包含性**：示例代码中的每个变量、函数、常量，其声明或来源必须在**同一代码块或紧邻的文字**中可追溯。非通识性外部符号（如 `gBS`、`gST`、`EFI_HANDLE`）首次出现时加注释注明来源库或定义位置

### 5.1 算法/流程的编号步骤

**训练流程、推理算法、硬件初始化序列等过程性内容，必须用编号步骤呈现。**

- 每步一个动作，步骤间有逻辑顺序
- 步骤中的关键变量在步骤内或紧邻文字中解释
- 编号步骤后可附伪代码，但伪代码不能替代编号步骤（编号步骤更易读）
- 正例：
  ```
  1. 从数据集采样一张图 x_0
  2. 随机选一个时间步 t（比如 t=500）
  3. 查表得到 ᾱ_500 = 0.10
  4. 采样噪声 ε
  5. 计算 x_500 = √0.10 · x_0 + √0.90 · ε
  6. 把 x_500 和 t=500 输入网络，得到预测噪声 ε̂
  7. 计算损失 = ‖ε - ε̂‖²
  8. 反向传播，更新参数
  ```
- 正例：
  ```
  1. 上电，等待 PLL 锁定（检查 LOCK 位）
  2. 配置时钟分频寄存器（CLK_DIV = 0x3）
  3. 使能 DDR 控制器（CTRL.EN = 1）
  4. 等待初始化完成（轮询 STAT.DONE 位）
  5. 执行 ZQ 校准（ZQ_CAL = 1，等待 ZQ_DONE）
  6. 进入正常工作模式
  ```

***

## 6. 数学公式

- **必须**使用 LaTeX 语法：行内公式用 `$...$`，独立公式用 `$$...$$`
- 独立公式前后各空一行
- 公式中出现的变量，在正文首次出现时用文字解释其物理含义
- 需要引用的编号公式使用 `$$... \tag{n}$$`，正文中以"由式 (n)"方式引用
- 禁止用纯文本或 Unicode 符号拼凑公式（如 `a^2 + b^2 = c^2`），必须使用 LaTeX 排版

### 6.1 公式后的逐符号解释

**每个独立公式首次出现时，必须逐个符号解释其含义。**

- 用无序列表逐个解释：变量名、维度、物理含义
- 正例：
  ```
  - x_{t-1}：上一步的数据（196608 维向量）
  - ε：从 N(0, I) 采样的随机噪声（也是 196608 维向量）
  - β_t：噪声调度参数，通常从 0.0001 逐渐增大到 0.02
  ```
- 反例：公式后直接进入下一个话题，读者自行猜测符号含义

***

## 7. 图片与图表引用

### 7.1 图片使用规范

- 图片必须与正文内容**紧密耦合**——不是装饰，而是论证的一部分
- 图片前后必须有文字解释其核心信息
- 引用图片时，使用以下格式标注来源：

```markdown
![图片描述](./images/filename.png)

*来源：Author et al., Year Figure X*
```

或

```markdown
![图片描述](./images/filename.png)

*来源：JEDEC Standard No. 21-C, Section X*
```

- 图片描述应简洁说明图片展示的内容
- 来源标注格式根据来源类型选择：
  - 论文：`*来源：Author et al., Year Figure X*`
  - 规格书/标准：`*来源：JEDEC Standard No. XX, Figure X*` 或 `*来源：ARM DDI XXXX, Figure X*`
  - 自制图：无需标注来源

### 7.2 "如何读这张图"引导

**复杂图片（架构图、流程对比图等）必须附阅读指引。**

- 在图片后用一段文字说明图中各部分含义、读者应关注的对比或趋势
- 正例："这张图的核心信息：AR 用因果注意力预测 next token；离散 DLM 用全注意力，随机掩码后预测原始 token……"
- 反例：图片后直接进入下一个话题

***

## 8. 核心要点标注

**文档中最重要的结论、关键发现、设计原理，必须用引用块突出标注。**

格式：

```markdown
> **核心要点**：……
```

使用场景：

- 对比分析后的核心差异总结
- 多个方案权衡后的设计选择理由
- 纠正常见误解的关键澄清
- 贯穿全文的核心论点

规则：

- 每个主要章节（H2）最多 2-3 个核心要点标注，避免滥用
- 内容必须是**有信息量的结论**，不是空洞总结
- 正例：`> **核心要点**：两者的网络结构（Transformer）完全相同，差异仅在注意力掩码、训练目标和生成方式。`
- 反例：`> **核心要点**：扩散模型很重要。`

***

## 9. Mermaid 图表

### 9.1 选型

| 要表达的内容       | 图表类型                        | 方向       |
| ------------ | --------------------------- | -------- |
| 流程 / 步骤      | `flowchart`                 | `TD`（默认） |
| 横向时间线 / 阶段演进 | `timeline` 或 `flowchart LR` | `LR`     |
| 组件间通信 / 请求链路 | `sequenceDiagram`           | —        |
| 状态迁移         | `stateDiagram-v2`           | —        |
| 层级架构 / 模块组成  | `flowchart` + `subgraph`    | `TD`     |
| 数据实体关系       | `erDiagram`                 | —        |
| 学习路径 / 甘特规划  | `gantt`                     | —        |

**原则：内容决定类型，不要把所有东西都塞进 flowchart。**

### 9.2 视觉设计

**布局**：

- 默认 `TD`（自上而下），仅在表达时间/阶段并列时用 `LR`
- 单图节点数 **5-15 个**，超出拆分为多图或 subgraph
- subgraph 按逻辑层/功能域分组，标题用双引号：`subgraph "控制层"`
- 避免交叉连线，必要时调整节点顺序

**节点形状（语义化）**：

| 形状    | 语法       | 语义           |
| ----- | -------- | ------------ |
| 矩形    | `[文本]`   | 处理 / 模块 / 概念 |
| 圆角    | `([文本])` | 起止状态         |
| 菱形    | `{文本?}`  | 判断 / 条件      |
| 平行四边形 | `[/文本/]` | 输入 / 输出      |
| 圆柱    | `[(文本)]` | 存储 / 数据库     |
| 双圆    | `((文本))` | 关键节点 / 事件    |

**颜色体系**（语义化 classDef）：

```
成功/正常:  fill:#d1fae5, stroke:#059669, color:#065f46    (绿色系)
警告/注意:  fill:#fef3c7, stroke:#d97706, color:#92400e    (黄色系)
错误/异常:  fill:#fee2e2, stroke:#dc2626, color:#991b1b    (红色系)
信息/说明:  fill:#cffafe, stroke:#0891b2, color:#155e75    (蓝色系)
处理/流程:  fill:#dbeafe, stroke:#2563eb, color:#1e40af    (蓝色系)
中性/默认:  fill:#f1f5f9, stroke:#64748b, color:#334155    (灰色系)
```

- 同一文档内颜色语义必须一致
- 最多 3-4 种 classDef，避免颜色过多
- `stroke-width: 2px` 统一

### 9.3 主题初始化（必须）

每个图表**必须**包含 `%%{init}%%` 配置，统一使用以下主题：

```
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#f8fafc", "primaryTextColor": "#1e293b", "primaryBorderColor": "#475569", "lineColor": "#64748b", "secondaryColor": "#f1f5f9", "secondaryBorderColor": "#94a3b8", "tertiaryColor": "#f8fafc", "fontFamily": "\"trebuchet ms\", verdana, arial, sans-serif"}}}%%
```

### 9.4 命名

| 项        | 规则         | 正例               | 反例                       |
| -------- | ---------- | ---------------- | ------------------------ |
| 节点 ID    | PascalCase | `StartProcess`   | `start_process`, `node1` |
| 节点标签     | 中文，简洁      | `[验证数据]`         | `[validate data]`        |
| 判断分支     | Yes/No     | `--> Yes -->`    | `--> y -->`              |
| 连接线      | 必须有标签或上下文  | `A --> 请求 --> B` | `A --> B`（无标签）           |
| Subgraph | 双引号包裹      | `subgraph "驱动层"` | `subgraph drv`           |

### 9.5 禁止

- 省略 `%%{init}%%`
- 节点 ID 用保留字（`end`, `loop`）
- 注释中包含花括号
- 无标签的孤立连接线

***

## 10. 交叉引用

- 同目录用相对路径 `[文本](./file.md)`，跨目录用 `../dir/file.md`
- 引用出现在正文中自然位置，不要都堆在末尾

***

## 11. 源码引用

引用 `zephyr-src/` 或 `edk2/edk2-src/` 时：

- 使用相对路径，标注文件路径和行号范围
- 摘录不超 50 行，超出标注省略
- 摘录前后**必须有文字解释**其作用

***

## 12. 技术准确性

- 数值（频率/带宽/电压/延迟）必须与官方规格书一致
- 不确定的内容用 `> **待确认**：XX` 标注，**禁止编造数据**
- 区分"规范定义"与"常见实现"

***

## 13. 文件命名

| 场景   | 规则             | 示例                           |
| ---- | -------------- | ---------------------------- |
| 有序章节 | `{序号}-{描述}.md` | `01-DDR基础概念.md`              |
| 英文命名 | kebab-case     | `bar-resource-allocation.md` |
| 导航文件 | `README.md`    | —                            |

同一专题内必须统一风格。

***

## 14. 审核清单

- [ ] H1 唯一，层级无跳跃，无 H5+
- [ ] 同专题编号风格统一
- [ ] 每个重要概念有本质解释（本质先行）
- [ ] 每个 H2 章节有桥接引用块（承接上文 + 说明动机 + 预告内容）
- [ ] 关键设计选择有"为什么"解释
- [ ] 独立公式首次出现时有逐符号解释
- [ ] 独立公式后有具体数值演算示例（仅当文档包含数学公式时）
- [ ] 复杂表格/图片后有"如何读这张表/图"引导
- [ ] 核心结论有核心要点标注（`> **核心要点**：`）
- [ ] Mermaid 图含 `%%{init}%%`，节点 ≤ 15，ID 用 PascalCase
- [ ] 表头加粗，代码块有语言标签
- [ ] 数学公式使用 LaTeX 语法，变量有文字解释
- [ ] 首次出现的缩写已定义，文档头部有"关键术语"表
- [ ] 图片有来源标注（`*来源：Author et al., Year Figure X*`）
