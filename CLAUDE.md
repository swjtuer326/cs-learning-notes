# CLAUDE.md — 学习笔记写作规范

指导 AI 在本仓库中编写、修改、审核学习笔记。目标,让每篇笔记都能回答四个问题——**这个东西在系统里干什么、为什么这样设计、代码怎么落地、与相邻实现怎么对比**。

## 适用读者与场景

本规范面向**嵌入式与系统软件工程师**,典型工作场景:

- RISC-V 服务器级芯片系统固件(SBI / UEFI / 启动链 / 电源管理)
- 专用 AI 加速芯片驱动与固件(UMD 用户态驱动 / KMD 内核驱动 / 卡上固件)
- ARM 与 NVIDIA 优秀软硬件设计的对照学习
- 组件交界处的问题定位(固件 ↔ 硬件 ↔ 驱动 ↔ OS)

> **核心要点**:读者在"组件交界处"工作,所以本规范特别强调三点——**软硬件耦合上下文**(1.8)、**源码引用聚焦设计而非实现**(1.7)、**官方文档可追溯**(2.6 / 4.3)。这三点不是可选项,是本规范区别于通用写作指南的核心。

***

## 0. 术语与缩写

**核心规则:任何非通识性英文缩写,首次出现时必须给出全称和中文释义。**

- 通识性缩写(可不定义):CPU, GPU, OS, API, RAM, USB, HTTP, HTML, CSS, SQL
- 其余所有缩写**必须定义**,格式:`DDR (Double Data Rate, 双倍数据速率)`
- **每篇文档**在标题下方、正文之前设置 `### 关键术语` 表格,列出本文涉及的核心缩写
- 若文档涉及多个项目/规范的对照(如 TF-A + OP-TEE + OpenSBI),关键术语表应包含**跨项目对照术语**(如 `PSCI (Power State Coordination Interface, ARM 电源管理接口) ↔ HSM (Hart State Management, OpenSBI 对应扩展)`),帮助读者建立映射

***

## 第一部分:写作原则

### 1.1 语言与语调

- **语言**:中文为主,英文术语首次出现带括号原文。语调:工程师对工程师,直接专业。
- **禁止**:过多废话("在当今..."、"让我们深入...")、空洞总结("综上所述...")、无信息过渡句、表情符号、过度谦虚("笔者水平有限")。
- **鼓励**:具体数据替代抽象描述、对比帮助理解、类比降低门槛(须说明局限)、引用规范注明章节号。

### 1.2 本质先行

**每个重要概念必须先理解本质,再引入形式化描述。**

- 先用具体场景解释"这件事在做什么",再用数学/代码/规范精确描述"怎么做"
- 场景说明须指出适用范围——读者需要知道该描述在何处失效
- 正例:先说"扩散模型的核心操作:把部分 token 替换为掩码,再从掩码恢复原始内容",再定义前向/反向过程的数学
- 正例:先说"Cache 把常用数据放在离 CPU 更近的小容量存储中,减少访问主存的次数",再讲 Cache 层级和命中率的数学定义
- 反例:上来就写 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon$,读者不知道这个公式在干什么

**驱动力与不变量**(项目类/系统类文档):概述段除"本质"外,还要给出领域的**驱动力链**(被什么根本矛盾推着走、矛盾如何接力)、**不变量**(演进中什么没变)、**走向**(当前瓶颈与业界方向)。正例:注意力机制被"长程依赖 → O(N²) → KV 带宽 → 长上下文效率"的接力问题推着走,不变量是"query 找 key 算权重再取 value"。反例:概述直接进入"本文从时间/结构/效率维度梳理"——维度罗列不是全景。位置见 [§2.1 骨架](#21-章节文档骨架)的"1.3 驱动力与不变量"。

### 1.3 "为什么"解释

**关键设计选择必须解释"为什么",不能只说"是什么"。**

- 引入新概念/新机制/新公式时,回答:为什么需要它?它解决了什么问题?不用它会怎样?
- 正例:"为什么掩码比例要随机?因为推理时模型可能面对任何掩码比例——从全掩码到几乎无掩码。训练时让模型见过各种比例,它才能在多步去噪的每一步都表现良好。"
- 正例:"为什么 DDR 需要预取(Prefetch)?因为 DDR 的行激活后数据只在行缓冲区中,如果不预取,每次列访问都要等 tCL 延迟——预取把连续 8 个列数据一次性取出,摊薄了延迟开销。"
- 反例:"掩码比例从 $U[0,1]$ 采样。"(没有解释为什么)

**代价与边界**(强制):讲完收益后必须补三问——**它牺牲了什么**(面积/功耗/带宽/表达力/复杂度)?**在什么条件下反而更差**?**它把什么新问题留给了下一层**?只讲收益不讲代价是宣传而非工程判断。正例:MLA 压缩 KV 省带宽,代价是额外的上投影矩阵乘;失效边界是头数少或压缩维度接近原维度时低秩假设退化;未解决 RoPE 与低秩压缩的数学冲突(需拆分技巧)。反例:"MLA 把 KV 压缩到 1/10,推理快 5 倍。"

### 1.4 具体数值演算

**当文档包含数学公式时,公式不应仅列出形式,应用真实数字逐步计算至少一个例子。**

- 适用场景:文档中有可计算的数学公式(如扩散过程、时序分析、功耗计算)
- 不适用场景:纯概念描述、寄存器字段定义、协议状态机等无计算公式的章节
- 给出公式后,用小规模数据走一遍完整计算过程,让读者能手动验证
- 数值例子放在公式之后、一般性讨论之前
- 正例:给出 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon$ 后,用 2×2 灰度图、具体 $\beta_1=0.0001$ 逐步算出 $x_1$ 的每个元素
- 正例:给出 DDR 行激活功耗公式后,代入具体频率和电压算出一个典型值
- 反例:只写公式,没有数值验证

### 1.5 具体小例子先行

**在解释一般性概念之前,先给出小规模、可手动验证的具体例子。**

- 例子规模要小到读者能在脑中或纸上验证(如 3-5 个 token、2×2 像素、4 个寄存器字段、3 步握手流程)
- 例子必须完整——输入、过程、输出都给出,不留悬念
- 之后再推广到一般情况

### 1.6 对比驱动理解

**当引入新概念时,优先通过与读者已知概念的对比来建立理解。**

- 对比表格第一列为"对比维度"或"维度"
- 对比维度应覆盖关键差异(如:输入/输出、训练目标/功能、推理方式/工作模式、优劣势)
- 正例:引入离散扩散时,与 BERT 的 MLM 对比(掩码比例、注意力、预测目标、用途)
- 正例:引入 DDR5 时,与 DDR4 对比(Bank 数、突发长度、供电电压、速率范围)
- 正例:引入 RISC-V OpenSBI 时,与 ARM TF-A 对比(特权级、调用接口、固件类型、Domain vs SPD)

**选型依据**(强制):多方案对比表必须回答"在什么条件下选谁"——可以是独立列(如"何时选它"),也可以是表后引用块 `> **选型依据**:……`。内容须为"条件 → 方案"映射,不是把"适用场景"当 feature 的敷衍写法。反例:只罗列"A 有 X、B 有 Y"的 feature catalog,读者无法据此选型。正例:MHA/MQA/GQA/MLA 表附"中小规模选 GQA,规模大到 KV 带宽是真瓶颈时选 MLA"的选型依据。

### 1.7 源码与设计的平衡

**源码引用服务于"论证设计决策",而非"复现实现细节"。文档不是代码走查报告。**

#### 1.7.1 引用目的

源码出现在文档中是为了让读者看见"设计如何在代码中落地",而不是替代阅读源码本身。每段摘录必须回答,**这段代码体现了什么设计决策?**

#### 1.7.2 引用优先级

| 优先级 | 引用对象 | 例子 | 何时引用 |
|--------|----------|------|----------|
| **高** | 接口与数据结构 | `plat_psci_ops_t` 结构体、`DECLARE_RT_SVC` 宏、寄存器字段定义 | 体现"通用代码如何调用平台代码"的关键契约 |
| **中** | 控制流与关键函数 | `runtime_svc_init()` 遍历链接器段建查找表 | 体现核心算法或调度逻辑 |
| **低(慎用)** | 逐行实现 | 普通 helper、boilerplate、错误处理 | 仅当实现本身就是论点时才引用 |

#### 1.7.3 选取原则

- 选取**体现设计决策的最小片段**;能体现"为什么这样设计"的代码优先
- 无关初始化、错误处理 boilerplate 一律省略,用 `/* ... 省略 ... */` 标注
- 摘录前后必须有文字解释"这段代码解决了什么设计问题",不能只贴代码

#### 1.7.4 三种呈现方式的选型

| 呈现方式 | 适用场景 | 示例 |
|----------|----------|------|
| 真实源码摘录 | 需要读者验证具体字段/行号(寄存器定义、结构体布局、链接器段) | `plat_psci_ops_t` 结构体的字段列表 |
| 简化伪代码 | 强调控制流而细节会干扰理解 | 启动链各阶段调用顺序 |
| 文字描述 + 表格 | 设计决策已可在表格中清楚对比 | "三个平台 PSCI 传输方式对比" |

#### 1.7.5 反模式(明确禁止)

- **代码走查式逐函数罗列**:把 `bl31_main.c` 从头到尾贴一遍,每段配一句"这里做了 X"
- **无解释的大段摘录**:>30 行且无重点标注的代码块
- **引用与正文论点无关的"完整上下文"**:为了"完整性"贴上一堆与当前论点无关的代码
- **维度罗列式文档**:按"时间/结构/效率/应用"等多个正交维度平铺展开,每个维度下罗列变体——每个变体只到"是什么"不到代价与边界,维度间割裂,读者无法形成选型力。正确做法见 [§2.3](#23-渐进复杂度):H2 是接力问题,变体作为对该问题的不同回答被对比

> **核心要点**:源码是论据,不是主角。读者读完一节应该记住"这里用了函数指针实现可移植性",而不是"这段代码第 47 行有个 `if` 判断"。本节讲"为什么引用、引用什么",格式规则见 [4.2 源码引用](#42-源码引用)。

### 1.8 背景与耦合上下文

**每篇文档不只讲"这个项目本身",还要讲"它在更大系统中的位置、与相邻组件的耦合、跨实现的差异"。**

#### 1.8.1 三层背景

每篇项目类文档(非纯概念文档)的概述章节必须覆盖以下三层背景:

1. **项目定位**:这个项目/技术在更大技术栈中的位置,解决什么生态问题,边界在哪里
2. **软硬件耦合点**:固件 ↔ 硬件 ↔ 驱动 ↔ OS 的接口与共同设计点
   - 例:DDR 训练涉及 控制器 + PHY + PCB + 电源 + 时钟,任一环节失败都会导致训练失败
   - 例:PCIe 枚举涉及 Host Bridge + ECAM + 配置空间 + 链路训练,固件与内核分工
   - 例:AI 加速器涉及 UMD(用户态) ↔ KMD(内核态) ↔ 卡上固件 ↔ 硬件,通过共享内存/寄存器/中断耦合
3. **跨实现/跨架构对比**:至少给出一个对比维度
   - ARM vs RISC-V(TF-A BL31 ↔ OpenSBI M-mode)
   - NVIDIA vs 主流(独立 TEE/驱动栈设计 ↔ 开源参考实现)
   - Linux vs Zephyr(主线驱动框架 ↔ RTOS 驱动框架)
   - UMD vs KMD(用户态驱动 ↔ 内核态驱动,职责切分)

#### 1.8.2 写作位置与形式

- 放在文档"概述"章节,通常在 1.1 前置知识之后
- 用一张**系统上下文图**(Mermaid subgraph)标注本文研究对象与相邻组件的接口
- 上下文图应体现数据流/控制流方向,而非静态堆叠

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#f8fafc", "primaryTextColor": "#1e293b", "primaryBorderColor": "#475569", "lineColor": "#64748b", "secondaryColor": "#f1f5f9", "secondaryBorderColor": "#94a3b8", "tertiaryColor": "#f8fafc", "fontFamily": "\"trebuchet ms\", verdana, arial, sans-serif"}}}%%
flowchart LR
    subgraph "上层"
        OS["OS / 用户态"]
    end
    subgraph "本文研究对象"
        Target["研究组件"]
    end
    subgraph "下层"
        HW["硬件 / 相邻固件"]
    end
    OS -->|"接口 A"| Target
    Target -->|"接口 B"| HW
```

#### 1.8.3 正例与反例

- **正例**:TF-A 文档应说明"BL31 作为 Secure Monitor 是 ARM EL3 的实现,RISC-V 对应 OpenSBI 的 M-mode 固件;两者都通过 SMC/ecall 暴露服务给上层 OS;差异在于 ARM 用 EL3 硬件特权级隔离,RISC-V 用 PMP/ePMP 做内存隔离"
- **反例**:开篇直接进入源码目录结构,读者不知道这个项目为什么存在、与相邻固件什么关系

> **核心要点**:本规范面向在"组件交界处"工作的工程师,耦合上下文不是装饰而是论证基础。一篇没有上下文的文档,读者能记住"代码长这样",但无法回答"为什么是这个组件来做这件事、换一个架构会怎样"。

### 1.9 工程实践要点与选型决策表

**适用范围**:系统类/选型类/流程类文档(如 PCIe 枚举、调度策略、注意力演进、DDR 训练、HAV 平台选型)应至少各用一次;纯概念小条目可不用。

#### 1.9.1 工程实践要点

在某机制的常规解释之后,可用 `**工程实践要点**` 起头的段落或编号列表,沉淀"做才知道"的判断——新手与生产级差距、反直觉的纪律、隐性成本。必须是**有信息量的判断**,不是"这很重要"的空话;每个 H2 最多 1 处。例(摘自 chip/03):"UVM 环境的价值不在组件齐全,而在三件事——参考模型独立于 RTL、约束驱动覆盖率收敛、跨层复用率 80%+"。

#### 1.9.2 选型决策表

多方案且选型依赖具体条件时,用 `场景/条件 → 推荐方案 → 理由(含代价)` 的表格,表后配 `> **核心要点(选型)**:……` 收束。必须有判断条件列与代价/理由列(即 [§1.6 选型依据](#16-对比驱动理解)的表格化形态);每篇最多 2 张。例(摘自 chip/03):HAV 选型表按"RTL 频繁改动 / OS 启动 / 软件 bring-up / 成本敏感"分档指向 Emulation / FPGA 原型,理由点出各自代价。

> **核心要点**:两者都强制配核心要点收束,避免"列了一堆条件但没有结论"。系统类/选型类文档缺了它们,读不出工程深度。

***

## 第二部分:文档结构骨架

### 2.1 章节文档骨架

```markdown
# 标题

> 一句话概括本文内容。
> **工程师视角**:(可选)实践角度补充。

### 关键术语
| 缩写 | 全称 | 含义 |
|------|------|------|
| XX | Xxxxx Xxxxx | ... |

---

## 1. 概述

### 1.1 前置知识
| 需要了解 | 参考文档 |
|----------|----------|
| XX | [链接](./xx.md) |

### 1.2 系统上下文
(项目定位 + 软硬件耦合点 + 跨实现对比 + 一张上下文 Mermaid 图,见 1.8 节)

### 1.3 驱动力与不变量
(项目类/系统类文档强制,见 1.2 节。三段:驱动力链 / 不变量 / 当前瓶颈与走向)

(正文,每 300-500 字插入视觉元素,图/表/代码)

---

## 官方文档索引
(可选,若本文引用了大量官方文档,单独成段;否则并入参考资料)

## 参考资料
- [名称](URL) — 参考了 §X.Y / Figure Z
```

> **核心要点**:相比旧骨架,新增"1.2 系统上下文"段(对应 1.8 节)、"1.3 驱动力与不变量"段(对应 1.2 节)与可选的"官方文档索引"段。前两者强制建立耦合背景与领域全景,后者把外部规范从末尾参考资料提升为正文导航。

### 2.2 章节桥接段

**每个主要章节(H2)开头应有一段桥接引用块**,完成三个任务:

1. **承接上文**:一句话概括上一章的核心结论
2. **说明动机**:为什么需要这一章?上一章留下了什么问题?
3. **预告内容**:本章将讲什么

格式:

```markdown
> 上一章建立了……的核心框架。一个自然的问题是:……?本章用……来回答这个问题——先讲……,再讲……。
```

正例:

```markdown
> 第二章讲了图像扩散:像素是连续实数,可以直接加高斯噪声。但文本是离散的——"猫"和"狗"之间没有中间状态。研究人员的第一反应是:能不能把文本变成"连续"的?这就是连续扩散语言模型的思路。
```

正例:

```markdown
> 上一章介绍了 DDR 的基本读写操作。但实际系统中,CPU 不会每次只访问一个 cache line——连续访问、页面命中/缺失、Bank 交错等因素共同决定了真实带宽。本章从时序图出发,分析 DDR 的带宽利用率。
```

反例:章节开头直接进入正文,读者不知道为什么突然切换话题。

### 2.3 渐进复杂度

**文档整体结构应从简单到复杂逐层递进,且必须问题驱动,不是维度平铺。**

- 先理解本质(具体场景、小例子),再引入形式化描述(数学/规范/代码),再讨论工程实现
- 先讲最简单/最基础的方案,再讲优化和变体
- 每一层只在前一层完全建立后才引入新复杂度
- **H2 应是一个接力问题/瓶颈,不是一个分类维度**——正例:"长程依赖怎么办 → O(N²) 怎么降 → KV 带宽怎么省 → 长上下文怎么扩";反例:"时间维度 / 结构维度 / 效率维度 / 应用维度"(见 [1.7.5 反模式](#175-反模式明确禁止))
- 正例:本质 → 图像扩散(连续,数学最简) → 连续文本扩散(引入 rounding 问题) → 离散文本扩散(主流方案) → 大规模模型
- 正例:SRAM 基本单元(6T 结构) → 读写操作 → 位线预充与灵敏放大 → 多端口 SRAM → Cache 组织方式

### 2.4 导航文档(README.md)

每个专题 README 必须包含以下要素:

1. 一句话定位
2. Mermaid 学习路线图
3. 文档索引表(序号/文档/概要/建议学时)
4. **官方文档表**(强制,见 2.6 节):文档名 / 用途 / 建议阅读时机
5. **源码导航表**(强制,若有 `src/` 子目录):仓库 / 路径 / 关键目录 / 职责 / 对应文档
6. 按角色推荐学习路径(可选)

### 2.5 导航链

每篇文档开头有"前置阅读",结尾有"下一篇"链接,保持双向可达。

### 2.6 官方文档与规范引用

**把"参考官方文档"从自觉行为升级为强制结构。** 大多数项目已有优秀的官方文档,笔记的职责是消化、对照、串联,而非替代官方文档。

#### 2.6.1 行内引用

正文中提到某个规范定义的概念、时序参数、寄存器字段、协议状态时,**在自然位置引用官方文档并标注章节号/图号**:

- `TBBR 规范(ARM DEN0006)§3.2 定义了启动链信任传递`
- `如 JEDEC Standard No. 79-4 Figure 11 所示`
- `DFI 4.0 规范 §5.1 定义了 phymaster 与 physlave 的握手时序`

#### 2.6.2 参考资料区

每篇文档结尾的"参考资料"必须包含官方文档,格式:

```markdown
## 参考资料
- [TF-A Firmware Design](https://trustedfirmware-a.readthedocs.io/en/latest/design/firmware-design.html) — 参考了 §3.2 运行时服务、Figure 4 描述符布局
- [JEDEC Standard No. 79-4](https://www.jedec.org/) — 参考了 §5.2 刷新时序、Figure 11 状态机
```

#### 2.6.3 README 强制表格

每个专题 README 必须有"官方文档"表:

| 文档 | 用途 | 建议阅读时机 |
|------|------|------|
| [TF-A Documentation](URL) | TF-A 完整文档 | 学完 04 后 |
| [TBBR Specification (ARM DEN0006)](URL) | 启动链信任传递规范 | 学完 03 后 |

#### 2.6.4 信息可信度优先级

> **核心要点**:信息可信度优先级——**官方规范(normative) > 官方文档(informative) > 论文 > 厂商应用笔记 > 博客 > 自制推断**。当来源冲突时以高优先级为准,并在文中标注冲突。

#### 2.6.5 区分规范与实现

明确标注"这是规范定义"还是"这是某实现的常见做法"。例:

- 规范定义:"JEDEC 规范要求 tRAS ≥ 32tCK"
- 实现差异:"Synopsys DWC 控制器额外要求 tRAS ≥ 33tCK,比规范保守 1 拍"

#### 2.6.6 本地源码文档

若专题有 `src/` 子目录(如 `trusted-firmware/src/tf-a-src/docs/`),优先指向本地路径,避免外链失效:

```markdown
- [TF-A Documentation (本地)](./src/tf-a-src/docs/) — 离线可读
```

***

## 第三部分:技术元素

### 3.1 标题层级

```
# 文档标题          ← H1,每文件仅一个
## 1. 大节标题      ← H2,每篇 5-8 个
### 1.1 小节标题     ← H3,每个 H2 下 2-4 个
#### 深入标题        ← H4,谨慎使用
```

- 禁止 H5+——拆为新 H3 或降为加粗文本
- 同一专题内编号风格统一(阿拉伯数字 `## 1.` **或** 中文序数 `## 一、`,不可混用)
- 标题末尾不加标点,不使用反引号

### 3.2 表格

- 表头**加粗**,内容关键词加粗
- 表格前后各空一行
- 对比类表格第一列为"对比维度"
- 超 8 行考虑拆分

#### 3.2.1 "如何读这张表"引导

**当表格包含多列数值或需要特定解读方式时,必须在表格后附一段阅读指引。**

- 用引用块格式:`> **如何读这张表**:……`
- 指出关键列的含义、数值的物理意义、读者应关注的趋势或对比
- 正例:`> **如何读这张表**:$\sqrt{\bar{\alpha}_t}$ 是原图信号在 $x_t$ 中的权重,$\sqrt{1-\bar{\alpha}_t}$ 是噪声的权重。$t=100$ 时信号占 $88\%$……`
- 反例:复杂表格后没有任何解释,读者自行摸索

### 3.3 代码块

- **必须**标注语言:`c`, `asm`, `bash`, `python`, `ld`, `yaml`, `json`, `dts`, `kconfig`
- 前后各空一行,超 30 行应拆分或标注省略
- 关键变量/函数在正文中有文字解释
- **自包含性**:示例代码中的每个变量、函数、常量,其声明或来源必须在**同一代码块或紧邻的文字**中可追溯。非通识性外部符号(如 `gBS`、`gST`、`EFI_HANDLE`)首次出现时加注释注明来源库或定义位置

#### 3.3.1 算法/流程的编号步骤

**训练流程、推理算法、硬件初始化序列等过程性内容,必须用编号步骤呈现。**

- 每步一个动作,步骤间有逻辑顺序
- 步骤中的关键变量在步骤内或紧邻文字中解释
- 编号步骤后可附伪代码,但伪代码不能替代编号步骤(编号步骤更易读)
- 正例:
  ```
  1. 从数据集采样一张图 x_0
  2. 随机选一个时间步 t(比如 t=500)
  3. 查表得到 ᾱ_500 = 0.10
  4. 采样噪声 ε
  5. 计算 x_500 = √0.10 · x_0 + √0.90 · ε
  6. 把 x_500 和 t=500 输入网络,得到预测噪声 ε̂
  7. 计算损失 = ‖ε - ε̂‖²
  8. 反向传播,更新参数
  ```
- 正例:
  ```
  1. 上电,等待 PLL 锁定(检查 LOCK 位)
  2. 配置时钟分频寄存器(CLK_DIV = 0x3)
  3. 使能 DDR 控制器(CTRL.EN = 1)
  4. 等待初始化完成(轮询 STAT.DONE 位)
  5. 执行 ZQ 校准(ZQ_CAL = 1,等待 ZQ_DONE)
  6. 进入正常工作模式
  ```

### 3.4 数学公式

- **必须**使用 LaTeX 语法:行内公式用 `$...$`,独立公式用 `$$...$$`
- 独立公式前后各空一行
- 公式中出现的变量,在正文首次出现时用文字解释其物理含义
- 需要引用的编号公式使用 `$$... \tag{n}$$`,正文中以"由式 (n)"方式引用
- 禁止用纯文本或 Unicode 符号拼凑公式(如 `a^2 + b^2 = c^2`),必须使用 LaTeX 排版

#### 3.4.1 公式后的逐符号解释

**每个独立公式首次出现时,必须逐个符号解释其含义。**

- 用无序列表逐个解释:变量名、维度、物理含义
- 正例:
  ```
  - x_{t-1}:上一步的数据(196608 维向量)
  - ε:从 N(0, I) 采样的随机噪声(也是 196608 维向量)
  - β_t:噪声调度参数,通常从 0.0001 逐渐增大到 0.02
  ```
- 反例:公式后直接进入下一个话题,读者自行猜测符号含义

### 3.5 图片与可视化

#### 3.5.1 可视化选型

**内容决定媒介,不要把所有东西都塞进 Mermaid 或都贴图。** 选型表:

| 要表达的内容 | 首选媒介 | 备选 |
|--------------|----------|------|
| 组件层级/模块组成 | Mermaid flowchart + subgraph | 自制分层图 |
| 时序/请求链路 | Mermaid sequenceDiagram | 复用官方时序图 |
| 状态迁移 | Mermaid stateDiagram-v2 | — |
| 数据实体关系 | Mermaid erDiagram | 表格 |
| 多维数值对比 | 表格 | 柱状图(自制) |
| 复杂架构/物理结构 | 复用官方图 | Mermaid(若可表达) |
| 算法/流程 | 编号步骤 + Mermaid flowchart | — |
| 寄存器字段/位域 | 表格 | 自制位域图 |
| 学习路径/甘特规划 | Mermaid gantt | — |

**原则**:

- 能用 Mermaid 自制则自制(可维护、可搜索、版本可控)
- 官方图更精确时(尤其时序图、物理结构、位域图)复用官方图
- 两者结合时,官方图必须配"如何读这张图"引导
- 同一信息不要图、表、代码三重复制

#### 3.5.2 图片使用规范

- 图片必须与正文内容**紧密耦合**——不是装饰,而是论证的一部分
- 图片前后必须有文字解释其核心信息
- 引用图片时,使用以下格式标注来源:

```markdown
![图片描述](./images/filename.png)

*来源:Author et al., Year Figure X*
```

或

```markdown
![图片描述](./images/filename.png)

*来源:JEDEC Standard No. 21-C, Section X*
```

- 图片描述应简洁说明图片展示的内容
- 来源标注格式根据来源类型选择:
  - 论文:`*来源:Author et al., Year Figure X*`
  - 规格书/标准:`*来源:JEDEC Standard No. XX, Figure X*` 或 `*来源:ARM DDI XXXX, Figure X*`
  - 官方文档:`*来源:TF-A Documentation, Firmware Design §3.2*`
  - 自制图:无需标注来源

#### 3.5.3 "如何读这张图"引导

**复杂图片(架构图、流程对比图等)必须附阅读指引。**

- 在图片后用一段文字说明图中各部分含义、读者应关注的对比或趋势
- 正例:"这张图的核心信息:AR 用因果注意力预测 next token;离散 DLM 用全注意力,随机掩码后预测原始 token……"
- 反例:图片后直接进入下一个话题

#### 3.5.4 复用官方文档/规格书/论文图片

**鼓励复用官方图**:官方图通常比自制更准确(尤其时序图、架构图、位域图、状态机),且能直接对应规范章节。

**复用三要求**:

1. **裁剪/标注**:裁剪到相关区域,必要时用红框/箭头标注重点(允许在图片上做标注后另存为 `*-annotated.png`)
2. **来源标注**:严格遵守 3.5.2 节格式,标注到具体 Figure/章节号
3. **紧耦合正文**:图片前后必须有文字解释;不能仅放图

**文件命名**:`{来源简写}-{主题}.png`,如 `tf-a-rt-svc-descs-layout.png`、`jedec79-4-fig11-refresh.png`、`arm-dwe-spi-fig3-timing.png`。统一存 `images/` 子目录。

**PDF 图片提取方式**(简述):

- 截图裁剪(适用于少量图,最简单)
- `pdfimages -all input.pdf prefix`(提取嵌入图片)
- `pdftoppm -png -r 200 -f N -l N input.pdf prefix`(按页渲染,适用于矢量图)

**版权注意**:仅用于个人学习笔记,不商用;规格书图片标注来源即可。

> **核心要点**:可视化选型遵循"内容决定媒介"——Mermaid 用于可维护的逻辑图,表格用于多维对比,官方图用于精确的时序/结构。复用官方图时三要求(裁剪/来源/紧耦合)缺一不可。

### 3.6 Mermaid 图表

#### 3.6.1 选型

| 要表达的内容 | 图表类型 | 方向 |
| ------------ | -------- | -------- |
| 流程 / 步骤 | `flowchart` | `TD`(默认) |
| 横向时间线 / 阶段演进 | `timeline` 或 `flowchart LR` | `LR` |
| 组件间通信 / 请求链路 | `sequenceDiagram` | — |
| 状态迁移 | `stateDiagram-v2` | — |
| 层级架构 / 模块组成 | `flowchart` + `subgraph` | `TD` |
| 数据实体关系 | `erDiagram` | — |
| 学习路径 / 甘特规划 | `gantt` | — |

**原则:内容决定类型,不要把所有东西都塞进 flowchart。**

#### 3.6.2 视觉设计

**布局**:

- 默认 `TD`(自上而下),仅在表达时间/阶段并列时用 `LR`
- 单图节点数 **5-15 个**,超出拆分为多图或 subgraph
- subgraph 按逻辑层/功能域分组,标题用双引号:`subgraph "控制层"`
- 避免交叉连线,必要时调整节点顺序

**节点形状(语义化)**:

| 形状 | 语法 | 语义 |
| ----- | -------- | ------------ |
| 矩形 | `[文本]` | 处理 / 模块 / 概念 |
| 圆角 | `([文本])` | 起止状态 |
| 菱形 | `{文本?}` | 判断 / 条件 |
| 平行四边形 | `[/文本/]` | 输入 / 输出 |
| 圆柱 | `[(文本)]` | 存储 / 数据库 |
| 双圆 | `((文本))` | 关键节点 / 事件 |

**颜色体系**(语义化 classDef):

```
成功/正常:  fill:#d1fae5, stroke:#059669, color:#065f46    (绿色系)
警告/注意:  fill:#fef3c7, stroke:#d97706, color:#92400e    (黄色系)
错误/异常:  fill:#fee2e2, stroke:#dc2626, color:#991b1b    (红色系)
信息/说明:  fill:#cffafe, stroke:#0891b2, color:#155e75    (蓝色系)
处理/流程:  fill:#dbeafe, stroke:#2563eb, color:#1e40af    (蓝色系)
中性/默认:  fill:#f1f5f9, stroke:#64748b, color:#334155    (灰色系)
```

- 同一文档内颜色语义必须一致
- 最多 3-4 种 classDef,避免颜色过多
- `stroke-width: 2px` 统一

#### 3.6.3 主题初始化(必须)

每个图表**必须**包含 `%%{init}%%` 配置,统一使用以下主题:

```
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#f8fafc", "primaryTextColor": "#1e293b", "primaryBorderColor": "#475569", "lineColor": "#64748b", "secondaryColor": "#f1f5f9", "secondaryBorderColor": "#94a3b8", "tertiaryColor": "#f8fafc", "fontFamily": "\"trebuchet ms\", verdana, arial, sans-serif"}}}%%
```

#### 3.6.4 命名

| 项 | 规则 | 正例 | 反例 |
| -------- | ---------- | ---------------- | ------------------------ |
| 节点 ID | PascalCase | `StartProcess` | `start_process`, `node1` |
| 节点标签 | 中文,简洁 | `[验证数据]` | `[validate data]` |
| 判断分支 | Yes/No | `--> Yes -->` | `--> y -->` |
| 连接线 | 必须有标签或上下文 | `A --> 请求 --> B` | `A --> B`(无标签) |
| Subgraph | 双引号包裹 | `subgraph "驱动层"` | `subgraph drv` |

#### 3.6.5 禁止

- 省略 `%%{init}%%`
- 节点 ID 用保留字(`end`, `loop`)
- 注释中包含花括号
- 无标签的孤立连接线

### 3.7 核心要点标注

**文档中最重要的结论、关键发现、设计原理,必须用引用块突出标注。**

格式:

```markdown
> **核心要点**:……
```

使用场景:

- 对比分析后的核心差异总结
- 多个方案权衡后的设计选择理由
- 纠正常见误解的关键澄清
- 贯穿全文的核心论点

规则:

- 每个主要章节(H2)最多 2-3 个核心要点标注,避免滥用
- 内容必须是**有信息量的结论**,不是空洞总结
- 正例:`> **核心要点**:两者的网络结构(Transformer)完全相同,差异仅在注意力掩码、训练目标和生成方式。`
- 反例:`> **核心要点**:扩散模型很重要。`

***

## 第四部分:引用与准确性

### 4.1 交叉引用

- 同目录用相对路径 `[文本](./file.md)`,跨目录用 `../dir/file.md`
- 引用出现在正文中自然位置,不要都堆在末尾

### 4.2 源码引用

引用 `zephyr-src/`、`edk2/edk2-src/`、`trusted-firmware/src/` 等本地源码时:

- 使用相对路径,标注文件路径和行号范围
- 摘录不超 50 行,超出标注省略
- 摘录前后**必须有文字解释**其作用——且解释须指出"这段代码体现了什么设计决策"(与 [1.7 源码与设计的平衡](#17-源码与设计的平衡) 呼应)
- 跨仓库引用时优先指向本地 `src/` 子目录的浅克隆(如 `./src/tf-a-src/`),避免外链失效

正例:

```c
/* 摘自 [tf-a-src/plat/qemu/common/qemu_pm.c](./src/tf-a-src/plat/qemu/common/qemu_pm.c) 第 229-240 行 */
static const plat_psci_ops_t plat_qemu_psci_pm_ops = {
    .cpu_standby              = qemu_cpu_standby,
    .pwr_domain_on            = qemu_pwr_domain_on,
    /* ... 省略其余字段 ... */
};
```

解释:这段结构体体现了"通用代码通过函数指针调用平台实现"的设计——`plat_psci_ops_t` 是通用 PSCI 库与平台代码之间的契约,平台只需填入自己的回调,无需修改通用代码。

### 4.3 官方文档引用格式

与 [2.6 官方文档与规范引用](#26-官方文档与规范引用) 呼应:2.6 讲结构与强制要求,本节讲具体格式。

#### 4.3.1 行内引用格式

| 场景 | 格式 | 示例 |
|------|------|------|
| 规范章节 | `规范名(编号) §X.Y` | `TBBR 规范(ARM DEN0006)§3.2` |
| 规范图 | `规范名 Figure X` | `JEDEC Standard No. 79-4 Figure 11` |
| 官方文档章节 | `文档名 §X.Y` | `TF-A Firmware Design §3.2` |
| 本地源码文档 | `[文档名](./src/xxx/docs/)` | `[TF-A Documentation (本地)](./src/tf-a-src/docs/)` |

#### 4.3.2 参考资料区格式

```markdown
## 参考资料
- [名称](URL) — 参考了 §X.Y / Figure Z
- [本地文档名](./src/xxx/docs/) — 离线可读
```

每条官方文档必须标注"参考了什么章节/图",不能只给链接。

### 4.4 技术准确性

- 数值(频率/带宽/电压/延迟)必须与官方规格书一致,**且在文中或参考资料标注来源**(如"JEDEC Standard No. 79-4 §5.2")
- 不确定的内容用 `> **待确认**:XX` 标注,**禁止编造数据**
- 区分"规范定义"与"常见实现"
- 不同来源冲突时,按 [2.6.4 信息可信度优先级](#264-信息可信度优先级) 取舍并在文中标注

***

## 第五部分:文件与命名

### 5.1 文件命名

| 场景 | 规则 | 示例 |
| ---- | --------------- | ---------------------------- |
| 有序章节 | `{序号}-{描述}.md` | `01-DDR基础概念.md` |
| 英文命名 | kebab-case | `bar-resource-allocation.md` |
| 导航文件 | `README.md` | — |
| 复用图片 | `{来源简写}-{主题}.png` | `tf-a-rt-svc-descs-layout.png`、`jedec79-4-fig11-refresh.png` |

同一专题内必须统一风格。

***

## 第六部分:审核清单

### 6.1 审核清单

### 结构与层级

- [ ] H1 唯一,层级无跳跃,无 H5+
- [ ] 同专题编号风格统一
- [ ] 每个 H2 章节有桥接引用块(承接上文 + 说明动机 + 预告内容)

### 写作原则

- [ ] 每个重要概念有本质解释(本质先行)
- [ ] 关键设计选择有"为什么"解释,且每个新机制/新方案回答了代价、失效边界、未解决问题(1.3)
- [ ] 多方案对比表有选型依据——在什么条件下选谁(1.6)
- [ ] 独立公式首次出现时有逐符号解释
- [ ] 独立公式后有具体数值演算示例(仅当文档包含数学公式时)

### 背景与耦合(对应 1.8)

- [ ] 每篇项目类文档概述含"系统上下文"段(项目定位 + 软硬件耦合点 + 跨实现对比)
- [ ] 概述含"驱动力链 / 不变量 / 走向"段(项目类/系统类文档,1.2)
- [ ] 概述含一张系统上下文 Mermaid 图,标注与相邻组件的接口

### 结构

- [ ] H2 是问题/瓶颈驱动,不是维度平铺(2.3 / 1.7.5)

### 源码与设计(对应 1.7 / 4.2)

- [ ] 源码摘录体现设计决策,无代码走查式罗列(对照 1.7.5 反模式)
- [ ] 源码摘录标注文件路径与行号,≤50 行,前后有解释

### 工程深度(对应 1.9,系统类/选型类文档)

- [ ] 至少 1 处工程实践要点或 1 张选型决策表;若用,每 H2 ≤1 处要点、每篇 ≤2 张决策表

### 官方文档引用(对应 2.6 / 4.3)

- [ ] 正文行内引用了官方文档/规范,标注章节号或图号
- [ ] README 含"官方文档"表与"源码导航"表
- [ ] 参考资料区每条官方文档标注"参考了 §X / Figure Y"

### 技术元素

- [ ] 复杂表格/图片后有"如何读这张表/图"引导
- [ ] 核心结论有核心要点标注(`> **核心要点**:`)
- [ ] Mermaid 图含 `%%{init}%%`,节点 ≤ 15,ID 用 PascalCase
- [ ] 表头加粗,代码块有语言标签
- [ ] 数学公式使用 LaTeX 语法,变量有文字解释
- [ ] 可视化选型合理(Mermaid/表格/复用图各得其所,对照 3.5.1)

### 图片与可视化(对应 3.5)

- [ ] 复用的官方/论文图片有来源标注且紧耦合正文
- [ ] 复用图片文件命名遵循 `{来源简写}-{主题}.png`

### 准确性

- [ ] 首次出现的缩写已定义,文档头部有"关键术语"表
- [ ] 图片有来源标注(`*来源:Author et al., Year Figure X*`)
- [ ] 数值有可追溯的官方来源
